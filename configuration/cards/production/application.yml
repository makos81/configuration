spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mini_bank
    username: kodilla_user
    password: kodilla_password
server:
  port: 8080
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
      database: mysql